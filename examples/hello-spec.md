# Hello fixture contract

This is an isolated executor demo, not a project requirement.

R1: `greet(name)` trims whitespace and returns `Hello, <name>!`.
R2: Empty or whitespace-only names raise `ValueError`.
Verify both behaviors with unit tests in `test_hello.py`. Use Python stdlib only; no network or external resources are required.
