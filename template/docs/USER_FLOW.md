# User flows

Status: awaiting project-specific planning.

For each in-scope goal, record the starting state, user action, expected result and completion state. Include relevant cancellation, retry, failure, empty-state and return paths. Only document branches that affect this product.

Link flows to requirement IDs and meaningful tests or real observations. If screens need a separate catalog, add SCREENS/SCREEN_FLOW from the optional catalog during planning.
