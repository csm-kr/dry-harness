# Architecture

Status: awaiting project-specific planning. No stack or directory layout has been chosen by this template.

## Boundaries and data flow
Record actual components, ownership, interfaces and the data flow needed for the requested product. Separate persisted state, transient state and external services where applicable.

## Technology and layout
Record the chosen stack, relevant directories and runnable development/verification commands. Use observed repository conventions when present. Do not assume a web framework or ORM.

## Constraints and evolution
Document current limitations and concrete reasons for significant choices in [ADR.md](ADR.md). Add optional DB/API/STATE documents only if their detail is useful.
